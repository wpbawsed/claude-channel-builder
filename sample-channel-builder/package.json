{
  "name": "sample-channel-builder",
  "version": "0.1.0",
  "description": "Claude Code Channel server — bridges HTTP webhooks to Claude Code sessions via MCP",
  "type": "module",
  "scripts": {
    "start": "bun run webhook.ts",
    "dev": "bun --watch run webhook.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  }
}
